let express,connectDB,dotenv;_46e‍.w("express",[["default",["express"],function(v){express=v}]]);_46e‍.w("./dbConnection/database",[["default",["connectDB"],function(v){connectDB=v}]]);_46e‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);// const express = require("express");


const app = express();

dotenv.config();

app.get("/", (req, res) => {
  res.send("Hello, GatherGro!");
});
// Connect to Database
connectDB();
const PORT = process.env.APP_PORT;
app.listen(PORT, () => console.log(`Server running on port ${PORT}`));
